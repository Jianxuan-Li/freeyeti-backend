[tool.poetry]
name = "freeyeti"
version = "0.1.0"
description = ""
authors = ["freeyeti"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
django = "^4.1.5"
wagtail = "^4.1.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
